/** @type {import('next').NextConfig} */
const nextConfig = {
  // Lets the first Vercel build succeed even if there are lint warnings.
  eslint: { ignoreDuringBuilds: true },
};
export default nextConfig;
